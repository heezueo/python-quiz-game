# 📝 프로젝트 평가 및 설계 설명

## 1. 클래스별 역할

### Quiz 클래스

`Quiz` 클래스는 하나의 퀴즈 문제를 관리하는 역할을 합니다.

문제 내용(question), 선택지(choices), 정답(answer), 힌트(hint)를 하나의 객체로 묶어서 관리합니다.

주요 메서드:

- `display()` : 퀴즈 문제와 선택지를 출력
- `check_answer()` : 사용자의 답과 실제 정답을 비교
- `to_dict()` : 퀴즈 객체를 JSON 저장이 가능한 딕셔너리 형태로 변환

### QuizGame 클래스

`QuizGame` 클래스는 전체 퀴즈 게임의 진행을 담당합니다.

메뉴 출력, 퀴즈 실행, 퀴즈 추가 및 삭제, 점수 계산, 점수 기록, JSON 데이터 저장 및 불러오기 등의 기능을 관리합니다.

즉,

- `Quiz` → 하나의 퀴즈를 관리
- `QuizGame` → 전체 게임의 흐름을 관리

하도록 역할을 분리했습니다.


## 2. 함수(메서드) 분리 기준

기능별로 메서드를 나누어 코드의 가독성과 유지보수성을 높였습니다.

### 입력 및 메뉴 처리

- `show_menu()` : 메뉴 출력
- `run()` : 사용자의 메뉴 선택에 따라 기능 실행

### 게임 진행

- `play_quiz()` : 퀴즈 출제, 정답 확인, 점수 계산

### 퀴즈 관리

- `add_quiz()` : 새로운 퀴즈 추가
- `show_quizzes()` : 등록된 퀴즈 목록 출력
- `delete_quiz()` : 퀴즈 삭제

### 데이터 관리

- `save_data()` : 데이터를 JSON 파일에 저장
- `load_data()` : JSON 파일에서 데이터를 불러오기

기능별로 메서드를 분리하여 특정 기능을 수정할 때 관련된 부분을 쉽게 찾을 수 있도록 했습니다.


## 3. Git 커밋 규칙

하나의 기능이나 작업 단위가 완료될 때마다 커밋하는 방식으로 작업했습니다.

커밋 메시지는 다음과 같은 형식을 사용했습니다.

`Feat: 기능 설명`

예시:

- `Feat: 퀴즈 추가 기능 구현`
- `Feat: 퀴즈 삭제 기능 추가`
- `Feat: 문제 수 선택 기능 추가`
- `Feat: 점수 기록 및 힌트 기능 추가`
- `Feat: add score history with timestamp`

이렇게 작성하면 각각의 커밋에서 어떤 기능이 추가되었는지 쉽게 확인할 수 있습니다.


## 4. 클래스와 함수의 차이

클래스는 데이터와 해당 데이터를 처리하는 기능을 하나로 묶어 관리할 수 있다는 장점이 있습니다.

이 프로젝트에서는 하나의 퀴즈에 문제, 선택지, 정답, 힌트가 함께 필요하기 때문에 `Quiz` 클래스로 묶었습니다.

반면 함수 또는 메서드는 특정 작업을 수행하는 데 적합합니다.

예를 들어 `show_quizzes()`는 퀴즈 목록을 출력하는 하나의 기능을 담당합니다.

따라서 이 프로젝트에서는 여러 퀴즈의 데이터를 객체로 관리하기 위해 클래스를 사용하고, 각각의 기능은 메서드로 분리했습니다.


## 5. JSON을 사용하는 이유

JSON은 사람이 읽기 쉬운 형태로 데이터를 저장할 수 있으며 Python의 리스트와 딕셔너리 구조와 쉽게 대응할 수 있다는 장점이 있습니다.

또한 별도의 데이터베이스를 설치하지 않아도 파일 하나로 데이터를 저장할 수 있기 때문에 간단한 콘솔 프로그램에 적합하다고 판단했습니다.

이 프로젝트에서는 `state.json`에 다음 데이터를 저장합니다.

- 등록된 퀴즈
- 최고 점수
- 점수 기록
- 점수 기록 날짜 및 시간

프로그램을 종료한 후 다시 실행해도 기존 데이터를 유지할 수 있습니다.


## 6. Git Branch와 Merge

Git Branch는 기존 코드에 영향을 주지 않고 새로운 기능을 개발하기 위해 사용합니다.

예를 들어 새로운 기능을 개발할 때 별도의 브랜치에서 작업한 후 테스트가 완료되면 `main` 브랜치에 병합할 수 있습니다.

Merge는 서로 다른 브랜치에서 작업한 내용을 하나의 브랜치로 통합하는 과정입니다.

서로 다른 브랜치에서 같은 부분을 수정했다면 충돌이 발생할 수 있으며, 충돌 내용을 수정한 후 다시 병합해야 합니다.


## 7. 퀴즈가 1000개로 증가할 경우

현재 프로그램은 JSON 파일에 퀴즈 데이터를 저장하고 프로그램 실행 시 데이터를 메모리에 불러오는 방식입니다.

퀴즈가 1000개 정도로 증가하더라도 일반적인 콘솔 프로그램에서는 사용할 수 있지만 데이터가 계속 증가하면 다음과 같은 문제가 발생할 수 있습니다.

- JSON 파일 크기 증가
- 프로그램 실행 시 메모리 사용량 증가
- 특정 퀴즈를 찾을 때 전체 목록을 검색해야 함
- 데이터가 많아질수록 파일 입출력 시간이 증가할 수 있음

데이터가 더 크게 증가한다면 SQLite와 같은 데이터베이스를 사용하는 방법을 고려할 수 있습니다.

데이터베이스를 사용하면 필요한 데이터만 검색하거나 조건을 이용해 데이터를 조회할 수 있습니다.


## 8. 데이터 백업 및 복구 전략

현재 프로그램은 `state.json`을 사용하여 데이터를 저장하며 파일이 없거나 JSON 형식이 손상된 경우 예외 처리를 통해 기본 데이터를 사용하도록 구성했습니다.

하지만 저장 파일 자체가 손상될 경우 기존 데이터가 사라질 가능성이 있기 때문에 실제 서비스에서는 백업 파일을 함께 관리하는 것이 좋습니다.

예를 들어 저장하기 전에 기존 파일을 `state.json.bak`으로 복사해 두는 방법이 있습니다.

### 백업 및 복구 과정

1. 기존 `state.json`을 백업
2. 새로운 데이터를 저장
3. 저장 과정에서 문제가 발생하면 백업 파일 사용
4. JSON 파일이 손상된 경우 백업 파일에서 복구
5. 백업 파일도 사용할 수 없다면 기본 데이터를 사용

현재 프로젝트에서는 기본적인 파일 없음 및 JSON 손상 예외 처리를 구현했으며, 향후 자동 백업 기능으로 확장할 수 있습니다.


## 9. 기능 변경 시 수정 위치

### 퀴즈 데이터 구조 변경

`quiz.py`의 `Quiz` 클래스를 수정합니다.

예를 들어 난이도 정보를 추가한다면 `Quiz` 클래스의 속성과 `to_dict()`를 수정해야 합니다.

### 점수 계산 방식 변경

`quiz_game.py`의 `play_quiz()`를 수정합니다.

현재는 정답을 맞힐 때마다 다음과 같이 점수를 증가시킵니다.

`score += 1`

### 점수 기록 변경

`play_quiz()`의 `score_history` 저장 부분과 `show_best_score()`를 수정합니다.

현재는 점수와 날짜 및 시간을 함께 기록합니다.

### JSON 저장 구조 변경

`save_data()`와 `load_data()`를 수정합니다.

저장하는 데이터 구조가 변경되면 저장과 불러오기 부분을 함께 수정해야 합니다.

### 메뉴 기능 변경

`show_menu()`와 `run()`을 수정합니다.

메뉴 항목을 추가하면 `run()`에서 해당 메뉴를 선택했을 때 실행할 메서드도 연결해야 합니다.